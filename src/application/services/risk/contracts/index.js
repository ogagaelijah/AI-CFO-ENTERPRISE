// src/application/services/risk/contracts/index.js

const {
    RiskContracts,
    RISK_TYPES,
    SEVERITY_LEVELS,
    RISK_STATUS,
} = require('./RiskContracts');

module.exports = {
    RiskContracts,
    RISK_TYPES,
    SEVERITY_LEVELS,
    RISK_STATUS,
};