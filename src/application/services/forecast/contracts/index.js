// src/application/services/forecast/contracts/index.js

const {
    ForecastContracts,
    FORECAST_HORIZONS,
    DATA_SUFFICIENCY,
    CONFIDENCE_LEVELS,
    FORECAST_METHODS,
} = require('./ForecastContracts');

module.exports = {
    ForecastContracts,
    FORECAST_HORIZONS,
    DATA_SUFFICIENCY,
    CONFIDENCE_LEVELS,
    FORECAST_METHODS,
};